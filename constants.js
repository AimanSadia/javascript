//we don't want a value of variable to change
//it is going to create all kinds of bugs in the application
//instead of variables we use a constant
//so the value of variable as the name implies can change
//so the value of constant cannot change
//we cannot reassign a constant
 
const interestRate = 0.3;
// interestRate = 1;
console.log(interestRate);